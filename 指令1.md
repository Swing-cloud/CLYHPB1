## Plan: 滑动解锁与验证

TL;DR - 在现有 `CLY生日/index.html` 中实现一个可触摸/可拖动的“滑动解锁”区域；滑动完成后用 `prompt()` 询问“我们相识的地方在哪里？（请输入全称）”，按用户提供的正确答案进行验证（不区分大小写且忽略前后空格）。验证成功后隐藏解锁界面并显示主祝福界面；失败则提示并重置滑块。

**Steps**
1. 在 `CLY生日/index.html` 中创建基础页面结构：暗色星空/渐变背景、一个覆盖全屏的解锁容器和一个隐藏的主内容容器。 (*depends on step 2*)
2. 添加滑动解锁组件的 HTML：轨道（bar）、滑块（thumb）和状态文字。轨道内会有进度色块用于视觉反馈。 (*parallel with step 3*)
3. 添加 CSS：深色浪漫星空或渐变背景、滑块样式、移动端响应式布局、触摸友好风格。确保使用 `meta viewport`。 (*parallel with step 2*)
4. 添加 JavaScript：
   - 绑定鼠标与触摸事件（`pointerdown`/`pointermove`/`pointerup` 或 `touch`）以实现从左向右拖动的逻辑。支持键盘可选（非必须）。
   - 在拖动中更新进度条颜色并把提示文字从“→ 滑动解锁 →”变为“✅ 松开解锁”当滑块接近右端。
   - 当滑块达到右端并释放时：弹出 `prompt()` 询问问题；按匹配规则验证答案：`广州市第五中学`，不区分大小写，去除前后空格。
   - 若验证成功：执行动画隐藏解锁区域，并显示主祝福界面（可替换内容）；若失败：提示“猜错啦，再试试看~”，并把滑块平滑复位到起点。
5. 无外部依赖：全部使用原生 HTML/CSS/JS，便于直接替换 `index.html`。
6. 验证与测试：在桌面浏览器与手机触摸环境（Chrome 移动模拟器）测试滑动、提示和复位逻辑。

**Verification**
1. 在桌面：用鼠标拖动滑块到最右端并释放，应看到 `prompt()` 问题，并按答案正确显示主界面；错误答案弹出提示并重置滑块。
2. 在移动端（Chrome 手机模拟或真机）：触摸拖动行为正常，视觉反馈与桌面一致。
3. 检查在不同屏幕宽度下滑块和背景的布局无溢出，且主界面初始为隐藏。

**Decisions / Assumptions**
- 正确答案（由你确认）为：`广州市第五中学`。
- 匹配规则：不区分大小写并忽略前后空格（用户选择）。
- 使用原生 `prompt()` 进行交互问题；如需自定义对话框（更美观），可改为自定义 modal（后续可扩展）。

**Further Considerations**
1. 是否需要支持多个正确答案或昵称/简称？（当前实现为严格全称匹配，忽略大小写/空格）
2. 是否希望提示或失败次数上限（例如三次后显示提示）？
3. 如果想要更漂亮的提示框，我可以改用自定义 modal（含动画、样式）。



Saved answers from user:
- 正确答案：广州市第五中学
- 匹配规则：不区分大小写并忽略前后空格
